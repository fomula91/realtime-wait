# pnpm 설치를 비대화형·재현 가능하게 고정한다.
# - confirm-modules-purge=false: node_modules 가 다른 매니저로 만들어졌을 때
#   "삭제할까요?" 프롬프트에서 멈추지 않고 진행한다(verify 가 CI/새 세션에서 정지하지 않게).
# - engine-strict=true: engines(node>=20)를 만족하지 않으면 설치 단계에서 명확히 실패한다.
confirm-modules-purge=false
engine-strict=true
